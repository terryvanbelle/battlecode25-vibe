# Alice — LEARNINGS.md (index)

What I would tell a fresh session before it touched anything. **One or two lines
per lesson**; the full entry and its evidence live in `LEARNINGS_ARCHIVE.md`,
which is grep-only and must never be read whole:

    grep -n "<pointer text>" LEARNINGS_ARCHIVE.md
    sed -n '<line>,<line+60>p' LEARNINGS_ARCHIVE.md

Pointers are heading fragments, not line numbers, so they survive edits to the
archive. `TRAINING_LOG.md` remains the evidence of record and the home of the
**closed-directions ledger** — grep that ledger by hypothesis name before
opening any direction (it has caught a re-open in progress three sessions
running).

Split 2026-09-09: this file was 2,971 lines and was read in full at every cold
start, ~40k tokens of the ~77k that mandatory re-reading cost per session.

---

## A. Game facts my lineage has measured (BC25)

- **Paint is the binding resource; chips are not, and have not been since iteration 2.** Chip mountains of $113k–$205k in every early game. -> `"The economy of Battlecode 2025"`
- **Coverage is a stock under attack, not a running total.** It peaks and *decays* (562‰@r500 -> 467‰@r2000) while the army grows. Never reason "we paint X/round so coverage grows". -> `"Coverage is a stock under attack"`
- **65–100% of unit deaths are paint starvation, not combat.** A unit at 0 paint takes −20 HP/turn and cannot act. -> `"The real degeneracy: a starvation treadmill"`
- **The absorbing state — the most expensive bug this lineage has had.** Tower paint funds spawning; a soldier costs 200, a mopper 100, income is 5–15/turn. So a drained tower can only ever afford the cheap unit and never saves back up. **Confirmed size-conditional 2026-09-09**: alice's towers average 145 paint each on small maps (below the soldier price) and 221 on large. -> `"The absorbing state"`
- **Towers are the master variable.** The r300 tower leader wins 13/14 in self-play and 79–81% cross-lineage. -> `"towers are the master variable"`
- **Bytecode is not a constraint for this bot.** Measured, not assumed. -> `"Instrument hygiene"`
- **A pinned resource is only pathological if capacity sits idle behind it.** The discriminator, because I misread a pinned curve once. -> `"A pinned resource is only pathological"`
- **The board saturates with PAINT by r200–400** — 0–4% of paintable tiles unpainted. There is no unpainted frontier; every remaining per-mille must be taken from enemy paint. -> log, `"THE FRONTIER DOES NOT EXIST"`
- **But the RUINS do NOT saturate, and I conflated the two for 44 iterations.** Measured over 74 tournament games: **42–79% of every map's ruins are still unbuilt at the round the game is decided, and ZERO of 142 observations had none left.** "Ruins saturate" came from ONE map, late, in self-play, and I used it to kill iterations 44, 45 and the 45 draft. Ground saturating with paint is not ruins being built. -> log, `"Ruins do NOT saturate"`
- **The conversion channel is the mopper, not the splasher** (691 unpaints vs 62 splashes on one map), and 26–38% of mops are handed back within **1 round** by an adjacent enemy. -> log, `"The conversion census"`

## B. Retractions — do not re-adopt these

- **RETRACTED: "unit population has an interior optimum set by a clumping tax."** -> `"unit population has an interior optimum"`
- **PARTLY RETRACTED: ruin supply does NOT cap towers.** The master-variable half stands; the "supply not discovery" half was inferred purely from self-play, where both sides shared the defect. Killed by the first round-robin. -> `"ruin supply does NOT cap them"`
- **PARTLY RETRACTED: the soldier paint decomposition was wrong by an order of magnitude.** Movement is still exploration; the "~8 of 200 paint becomes tiles" claim is false. -> `"the paint"` + `"decomposition below is WRONG"`
- **SUPERSEDED: the old binomial noise bands.** They overstated spread ~2x and made me hedge a real 2-sd accept. -> `"Instrument hygiene"`
- **RETRACTED: "b0 = 0, iteration 19's branch never fires."** It fires on 10.9% of mops; the zero was a 128-mop small-sample artefact. -> log, `"RETRACTS my own"`
- **RETRACTED: `mopSwing` is a 3-tile ground converter.** The method body has **no tile-paint write** — it drains *robot* paint. I costed it from the constant name and reordered a queue around it. -> `"a constant's NAME is not its semantics"`
- **RETRACTED: "my gauntlet cannot see the size axis."** One claim too wide: it cannot see whether the *gap to carol* closed, since both arms are mine. -> log, `"I overclaimed"`

## C. What decides whether a measurement means anything

- **The self-referential blind spot — the most important thing this lineage learned**, and it cost eleven iterations behind a 95.8% self-measured win rate. -> `"The self-referential blind spot"`
- **A replay counter means nothing until you have found its call site.** `MopAction == 0` was guaranteed a priori. -> `"Methodology lessons paid for"`
- **Read the engine, not your own digest of the engine.** -> `"Methodology lessons paid for"`
- **The engine DEBITS before it CHECKS — verify the predicate, not the proxy.** -> `"the engine DEBITS before it CHECKS"`
- **A REPLAY's per-robot state is post-decision by construction**, so it cannot price affordability. -> `"post-decision by construction"`
- **A correlate you measured in a replay is not a predicate a robot can evaluate.** -> `"not a predicate a robot can evaluate"`
- **An instrument that samples positions your current policy chooses cannot price a policy that chooses different positions.** -> `"cannot price a policy that chooses different positions"`
- **An instrument can produce NOTHING and no-finding, and the two are indistinguishable downstream.** -> `"an instrument can produce NOTHING"`
- **A distribution measured UNDER a gate is shaped BY that gate.** To size a threshold you must MOVE it, never read the pile-up it produced. This nearly re-opened a three-times-closed direction. -> `"shaped BY that gate"`
- **An UNSPENT SURPLUS is not evidence of waste** — and my first correction of this was also wrong. -> `"an UNSPENT SURPLUS is not evidence of waste"`
- **A rate pooled over HETEROGENEOUS producers measures specialisation, not shortage.** -> `"HETEROGENEOUS producers"`
- **A counter that goes to ZERO late in a game may be a PHASE CHANGE, not a failure.** -> `"may be a PHASE CHANGE"`
- **A monotone counter is a bug report.** -> `"Instrument hygiene"`
- **An INERT BRANCH is a fact about the BRANCH.** Measure what the turns it fails to serve are actually *doing* — this is the iteration 44 lesson and it cost −58. -> `"an INERT BRANCH is a fact about the BRANCH"`
- **Measure whether the THING the mechanism points at EXISTS, before building the mechanism.** -> `"before building the mechanism"`
- **Three arms failed on one root: the bot had no representation of the thing they all guessed at.** -> `"the bot had no representation"`
- **Measure WHICH guard binds before fixing a mechanism that never fires.** -> `"measure WHICH guard binds"`
- **Enumerate the cases, then COUNT them** — a case analysis has no frequencies in it. -> `"a case analysis has no frequencies"`
- **When a covariate analysis fails twice, stop correlating and go price the code path.** -> `"stop correlating and go price the code path"`
- **Never dichotomise a continuous covariate** — the split invents the effect. -> `"never dichotomise a continuous covariate"`
- **A maximum-per-observation is not a total**, however many observations you add up. -> `"is not a total"`
- **You cannot compare a COMPOUNDING resource to a CONSUMPTIVE one with a per-turn rate.** -> `"COMPOUNDING resource"`
- **An exactly symmetric statistic is a hypothesis about the FILE FORMAT.** -> `"a hypothesis about the FILE FORMAT"`
- **One map cannot size a corpus quantity** — and I made this error inside the session I wrote the warning. -> `"one map cannot size a corpus quantity"`
- **Do not read a dump until its writer has exited.** Partial output is a biased sample of the *early game*: I computed 41% off a partial file whose finished value was 4.1%. -> log, `"caught within minutes"`

## D. Gates, doses and verdicts

- **In a deterministic game the MAP is the unit of uncertainty — never the game.** -> `"the MAP is the unit of uncertainty"`
- **A SCREEN ranks arms; only a CENSUS sizes an effect**, and small effects are where skipping the census tempts most. -> `"only a CENSUS sizes an effect"`
- **A CENSUS is exact** — separate sampling noise from run-to-run noise before calling something unresolvable. -> `"a CENSUS is exact"`
- **Measure your own noise floor, and check its MEAN.** A change with **zero policy content** (a PRNG seed offset) moved my census by **+12 net swept**. Any census bar at +4 would have accepted it. -> `"NO policy content moved my census by +12"`
- **My gate and my floor must be in the same unit** — the audit and derivation. Standing bars: screen (25 maps/50 games) net swept >= +4; census (75 maps/150 games) net swept >= **+12** (2.27 sd on `sd_net_swept = 5.29`). -> `"my gate and my floor are in the same unit"`
- **Swept maps and the head-to-head margin are THE SAME NUMBER**, not two agreeing signals. What sweeps add is D, the count of split maps. -> `"THE SAME NUMBER, not two signals"`
- **SW+SL and SW−SL are different measurements**, and a zero-variance null makes the first look like evidence. -> `"different measurements"`
- **Head-to-head margins DO NOT CHAIN.** Measured at cell level. -> `"DO NOT CHAIN"`
- **A tournament run is not an independent sample** — dedupe on the COMMIT PAIR, not the run id. -> `"dedupe on the COMMIT PAIR"`
- **With a deterministic engine, the SPLIT/SWEEP structure reads a mechanism's firing rate directly.** -> `"reads a mechanism's firing rate directly"`
- **The mechanism can engage exactly as designed and still invert the result.** -> `"Methodology lessons paid for"`
- **A BITE check proves a mechanism ACTS** — it can still invert the sign on the mechanism's own metric. -> `"a BITE check proves a mechanism ACTS"`
- **The arm that improves your metric most can be the arm that loses.** -> `"can be the arm that loses"`
- **A ratio is not a quantity.** Maximising "share of mops that stick" halved the ground held. -> log, `"A ratio is not a quantity"`
- **The CONSERVATIVE dose is not automatically the safe one.** -> `"the CONSERVATIVE dose is not automatically"`
- **A stronger dose producing LESS of the predicted effect is how you catch a noise reading.** -> `"producing LESS of the predicted effect"`
- **A pre-registered criterion may only cite quantities from the run it judges.** -> `"may only cite quantities from the run it judges"`
- **A dose comparison advances only after replication on a DISJOINT map sample.** -> `"Instrument hygiene"`
- **A hand-picked standing map list is an overfitting surface.** Leave `MAPS` unset; resample every run. -> `"Instrument hygiene"`
- **Don't pick the arm that won by less than the noise floor.** -> `"Methodology lessons paid for"`
- **A concentrated regression inside a broad win is a target, not a veto.** -> `"Methodology lessons paid for"`
- **Instrument any pool the change draws on, in the first run.** -> `"Methodology lessons paid for"`
- **Rejections are the cheapest evidence available.** -> `"Methodology lessons paid for"`
- **My gauntlet's resolution collapses exactly where I need it most.** -> `"resolution collapses exactly where I need it"`
- **ROUND-COUNT IDENTITY is not byte-identity** — the tiebreak forges the evidence. -> `"ROUND-COUNT IDENTITY is not byte-identity"`
- **A policy keyed on map geometry needs a map that exercises both branches.** -> `"needs a map that exercises both branches"`
- **A contested quantity sits at an equilibrium — measure throughput, not level.** -> `"measure throughput, not level"`
- **Removing waste pays only when the freed resource is the scarce one.** Iterations 20, 26, 39a each freed a non-binding resource; 44 spent the binding one to buy the non-binding one. -> `"removing waste pays only when"`
- **Pick the run convention that makes the reading unambiguous.** -> `"makes the reading unambiguous"`

## E. Keeping myself honest

- **R2 collapses into R1: the soldier is CORRECTLY IDLE.** On 81.0% of turns its action goes unused, and on **98.2% of those there is no EMPTY tile in reach** (mean 0.122) — and a soldier gains coverage only on an empty tile. So "spend the tank faster" has no mechanism: acting more often is blocked by availability, spawning fewer is arithmetically worse (paint's only sink is spawning), culling recovers nothing (`disintegrate` throws and returns no paint), and cutting the rate is the closed 7–17% family. **This bot already plays its local information near-optimally; the information is the limit.** -> log, `"R2 PRICES OUT"`
- **A ceiling on a counterfactual is not a lever — enumerate the routes to it before believing the number.** "+10.5 actions if the tank were spent in 40 turns instead of 88" was the largest figure of the session and had **no mechanism behind it**. Three routes died to measurement and one to engine arithmetic, all for zero games. -> log, `"a ceiling on a counterfactual"`
- **PLATEAU, measured: thirteen accepted iterations are worth less than my bar for one.** `alice_iter43` vs `alice_iter30`, 150 games: **+10 net swept (1.89 sd)** against a census accept bar of **+12** for a *single* iteration. Uniform across map size (rho −0.112, p 0.322). Not "no gain" — but the whole span since iteration 30 carries under one iteration's worth of evidence, which is what a local optimum reads as from the outside. -> log, `"worse than it looks positive"`
- **The two structural requirements every closure bottoms out in.** (R1) **A unit must act on information beyond its own r²=20 vision** — mean open ruins in vision is **1.00**, so no shared tie-break can converge units; completion needs a 4.5-soldier relay and gets 2.41; and messaging, the only cross-vision channel, is closed *both* ways. The requirement is persistent accumulated state or vision that overlaps by construction, **not** communication. (R2) **Unit lifetime must be coupled to available work** — soldiers idle 87.5% of turns, live 87.8, convert 50.6% of tank, and upkeep is `lifetime × rate`, so a long-lived idle unit is pure loss. -> log, `"two requirements, measured, not a design"`
- **Ask whether the LOOP is still producing, not just whether the last mechanism worked.** Deliberate roster runs show alice rising steeply to ~iteration 28 (flood 62→98, iter12 54→96) and then **flat**: the one census-powered comparison across the span since is **+2 over 150 games**. ~15 iterations, no measurable absolute gain — which is the missing explanation for why every mechanism priced at 7–17% and nine directions closed. **A plateau on the absolute instrument is what a local optimum looks like from outside.** -> log, `"is this lineage still gaining"`
- **"Cannot be told apart at census power" is a POSITIVE finding, not a null.** If iteration 43 is indistinguishable from iteration 30 over 150 games, that is the strongest available evidence that incremental work has stopped paying — better than any argument for changing track. Register that branch as a result before the run, or it reads as a disappointment afterwards. -> log, `"it is not a null"`
- **Measure a chain JOINTLY — multiplying marginals changed my decision by 2.3x.** "Can message a tower" is **34.0%** as a marginal but **15.1%** conditional on seeing an under-manned ruin. Naive product through that step: **2.10%, above my 2.0% bar and a licence to build.** Joint measurement: **0.93%, below it.** Not a hypothetical benefit of the rule — the rule flipping the verdict on a real iteration. -> log, `"would have passed the gate and licensed a build"`
- **Being where the WORK is means being where the TOWERS are not.** A soldier that can see an open, under-manned ruin is at the frontier by definition; messaging needs a 4-adjacent ally-paint path to a tower, i.e. being inside territory. The two conditions are negatively correlated **by construction**, so the comms channel is systematically unavailable exactly when there is something worth saying. **Messaging closed on delivery after 60 iterations as "the largest unused capability".** -> log, `"negatively correlated"`
- **A unit exactly ON a feature and a unit NEAR it are different geometry — one tile decides a theorem.** No two ruins in the 75-map corpus are mutually visible (min d² = **25** vs vision **20**), so a **tower**, which stands exactly on a ruin, can *never* see another. But a **soldier** sits at d²<=2 from a ruin, so a second ruin reaches d² >= 12.9 — inside vision. I proved the soldier case as a theorem once and was **wrong**; the tower case is where it actually holds. -> log, `"Same geometry, opposite conclusions"`
- **A pre-registration fixes which branch fires; it does not certify that the branch's ACTION is sound.** My counter fired "re-site into the slide", and executing it would have been wrong — the slide offers 79 opportunities/game against an already-inert site's 1,884. Fire the branch, check the prescription against evidence you already hold, and record why you are not executing it. Otherwise a registration becomes a licence to do the wrong thing on schedule. -> log, `"my own log refutes its prescription"`
- **When two of your own numbers disagree by 900x, trace the original — do not pick a side.** My log said the slide "fires 4 times a game"; my probe counted 3,774/game. They reconciled exactly: the original was a **funnel** (slide runs 691 → both candidates legal 79 → candidates differ 4), and my 36.0% matched its 30.3%. **I nearly filed a correction against a number that was right** — and the funnel then refuted the prescription I had just registered. -> log, `"They reconcile exactly"`
- **The site must be one where a DECISION exists, not merely where the code runs.** 64% of wander entries follow an *open heading* — the bot moves along it and that is the whole decision. So "a real crowd choice exists on 44.8% of movement turns" was measured over **hypothetical 8-direction moves**, not over choices this bot makes. The denominator lesson, one level deeper than the call-site version. -> log, `"where there is no choice to make at all"`
- **Measure the denominator the MECHANISM will actually see — not a superset of it.** Three times now: lever B priced mopper *surplus* and not mopper *adjacency*; iteration 56 priced tower *opportunity* and not the *paint* it spends; iteration 58 measured the crowd choice over **all** movement turns and then installed only in `tryMove`. Run the pre-check at the exact call site the code will live at. -> log, `"the same miss as lever B, a third time"`
- **Ask what the null is a null OF — the same simulation can mean opposite things.** Iteration 50's best-of-8 sector score was a *prediction* about 25 future turns, so the winner's curse bit fully and the headroom was illusory. Iteration 58's min-of-N crowd is a choice among tiles the unit *stands on next turn*, so selecting the minimum of N **realised** values is a mechanical gain, not a bias. Same statistic, same simulation, opposite reading — the estimand changed from a prediction to a realisation. -> log, `"opposite of what it said last time"`
- **Choose a bar statistic that needs no null.** I registered the crowd pre-check on `max − min >= 1` — a **spread** — precisely so the pass could not be an artefact of selection, and kept the best-of-N headroom as a secondary that had to clear a simulated null before being quoted. Pick the unbiased statistic for the gate and the biased one for colour, never the reverse. -> log, `"carries no winner's-curse bias"`
- **A multi-cell instrument read once is a multiple-comparisons problem — and its sibling cells are NOT corroboration.** My roster scores 12 frozen opponents; I read the worst cell (−2.29 sd) as a regression finding, when **P(at least one of twelve that low) = 0.121** — one run in eight. Then I "corroborated" it with the two largest movers **from the same twelve**, across runs on different map samples, while quoting the charter line forbidding exactly that. **Selecting the movers and calling them independent support is circular; they are the same draws.** An alarming cell is a **trigger for a census**, never a finding. -> log, `"the most alarming of twelve comparisons"`
- **Register the ACTION, not just the metric — it is what stops you acting on noise.** I wrote "≤ −12 revert / −11..−1 inconclusive / ≥ 0 stands" before the census; the result landed at **+2** and reverting HEAD on the screen alone would have destroyed a correct accept. Iteration 43 was then vindicated at 150 games, reproducing its pre-registered map cut exactly (+3 on the four, −1 on the other 71). -> log, `"The pre-registered rule did its whole job"`
- **A STALE absolute-strength instrument is worse than an absent one.** Mine was four accepts behind and its last reading silently licensed the belief the bot was improving — while iteration 43 was in fact **losing to iteration 39 by −7 net swept**. Every other instrument I own is relative and cannot separate "the bot improved" from "the instrument moved". Tied to accepts now, not convenience: `tools/roster-stale.sh`, which exits non-zero. -> log, `"MY CURRENT BOT IS A REGRESSION"`
- **Terrain-seeking and crowd-avoidance point in OPPOSITE directions with respect to finding work.** Steering toward cheap terrain moves a unit toward its *own paint*, which it cannot score on; steering away from crowd moves it away from its *own units*, and unworked ground is by definition where your units are not. My lever A lost 125 paint actions doing the first; bob's scored +6 three times doing the second. Same family, same named payer, opposite sign — the failure was **objective**, not dose. -> log, `"explains the sign flip"`
- **Reconcile UNITS before comparing another lineage's price to your bar.** `wins_above_half = W − N/2` and `net swept = W − N` happen to coincide on a 25-map/50-game screen and differ everywhere else. His family ceiling of +6 sits *below* his +10 bar and *above* my +4 — a fact that is only visible once the units are shown to match, and a factor-of-two error away from being invisible. -> log, `"the unit reconciles to mine"`
- **Registering WHAT to measure is not registering WHAT WOULD KILL IT.** Every pre-check today that gave a clean verdict had a threshold written before the number — lever B failed at 1.2%, iteration 56 passed a 2.00% bar at 2.23%. For the relay pre-check I registered only the measurement, so I could not claim a pre-registered pass and refused to invent a bar with the number on screen. Write the kill condition in the same breath as the metric. -> log, `"forgot to register the BAR"`
- **Do not reach for the nearest existing figure — measure the population you are about to build for.** I justified a direction with "53–65% of robot turns reach a tower"; that was ALL robots at two sampled rounds, and the soldier-specific whole-game number is **34.0%**. Not a contradiction, but the wrong population, and I quoted it because it was already in my log. -> log, `"I cited the wrong number one entry ago"`
- **The coordination gap is a VISION gap, not a decision gap — so no local rule can close it, by construction.** Alice's soldiers do not fail to concentrate because they *choose* differently; they see **1.00 open ruins each** (15,229 turns, choice set = 2 on zero of them), so two soldiers five tiles apart have disjoint target sets. A shared tie-break has no tie to break. This is *why* every local mechanism priced at 7–17%, not merely another instance of it. -> log, `"a VISION gap, not a decision gap"`
- **`mark` does not extend RANGE, only persistence.** A ground mark is read by a unit already close enough to see what it marks, so it buys time, not reach. Messaging is the only BC25 capability that crosses r²=20 — and its delivery was measured long ago at 53–65% of robot turns in range of a tower. The blocker was never delivery; it was the payload. -> log, `"does not extend range"`
- **BOTH of this lineage's fronts are ONE defect: no mechanism CONCENTRATES units on a target.** Completing a tower pattern needs a 4.5-soldier relay and gets 2.41; killing a tower needs 1.6 soldiers' whole paint budgets on one target and gets **1.01** soldiers in range. The per-tower production deficit and the collapse-from-ahead are the same missing capability seen from two ends — which is why every mechanism priced this session came in at 7-17%: they were all **local** rules, and the defect is **coordination**, which no local rule supplies. -> log, `"it unifies the two fronts"`
- **Change a registered measurement only with the reason stated FIRST, and only when the registered one is uninformative by construction.** I registered "measure the HP distribution of towers in range"; nothing alice owns ever damages a tower, so they all sit at full HP and the answer was fixed before the run. Concentration was the deciding quantity. Say why before running, never after seeing. -> log, `"uninformative by construction"`
- **"The turn is free" is not "the resource is free."** I registered a soldier tower-attack as *non-diverting by construction* because it fires only on turns whose action went unused — but the attack costs 5 paint, and **paint**, not turns, is the binding resource. Priced, the naive version consumes **70% of alice's entire soldier paint budget**. Ask which resource a mechanism spends, not which slot it occupies. -> log, `"The TURN is free. The PAINT is not"`
- **A "zero" measured on a selected sample is a property of the sample until you check.** I claimed alice destroys zero enemy towers and called the absence structural; the matched WIN sample found 7. The genuinely structural claim was narrower and sharper: **all of alice's tower damage comes from splashers, none from soldiers.** Run the same measurement on the complementary sample before promoting an absence to a property of the bot. -> log, `"corrects my own"`
- **A soldier is 5x more paint-efficient at killing towers than a splasher** (10.0 vs 2.0 damage per paint) — and alice does 100% of its tower damage with the splasher. Efficiency per unit of the *binding* resource is the comparison that matters, not damage per action. -> log, `"more paint-efficient at killing towers"`
- **An API-usage sweep catches unused METHODS, never an unused TARGET.** My sweep ran four times and always reported `attack` as *used* — it is, constantly, to paint. It could not see that **no alice soldier has ever targeted an enemy tower**, despite dealing 50 damage to one. Alice destroyed **zero** enemy towers across eight games while losing 109. When the verb is in constant use, sweep the *objects* it is called on. -> log, `"API-usage sweep catches unused"`
- **When you learn what kills you, immediately ask whether you do it back.** The tower-death attribution answered its own question (97% enemy soldiers, 0% enemy towers) and killed my defense-tower rationale — and the *inverse* measurement, which cost one more script, found a 25:1 asymmetry and a shutout. The symmetric question is nearly free and was the larger finding. -> log, `"the symmetric measurement is the real finding"`
- **Plot the master variable past the round you stopped at.** I established "the r300 tower leader wins" and then only ever measured towers AT r300. Traced past it, alice's tower count PEAKS and DECLINES in 6 of 6 collapse games while the opponent's rises monotonically — so the "coverage collapse" carried as unexplained since iteration 42 is downstream of tower loss, and every mechanism I aimed at that curve was aimed at paint. **48% of alice's losses are games it led at r300.** -> log, `"it is TOWER LOSS, not a coverage bleed"`
- **Ask whether you were ever ahead, when you lose.** Decomposing losses by position at the decisive round cost nothing and reframed a whole session: the expansion work explains the 30% of losses from behind, while the larger half are games where expansion already worked. A loss rate says nothing about *which* phase failed. -> log, `"COLLAPSES FROM AHEAD"`
- **When a mechanism barely fires, count the funnel IN ORDER — do not fix the first plausible cause.** Lever B fired 1%, and the obvious inference ("my threshold is too high") was wrong: 60% passed the dose step and only **20%** passed adjacency. Removing the dose entirely would have lifted firing 1.2% -> 1.9%, so a re-dose was the wrong knob and would have cost a build. The step with the smallest share-of-previous is the answer; a funnel that predicts the observed firing rate also validates itself. -> log, `"the collapse is ADJACENCY, not the dose"`
- **A pre-gate needs a NULL ARM, it is free, and it ships in the same batch.** Two instances: (1) a lever that provably did nothing still moved a 6-game production comparison by **−69 paint actions**, so that floor is ±70 — learned by accident; (2) run deliberately, arm-vs-control crowd moved **+6.4%** while **two byte-identical bots moved −7.7%**, so the effect was inside the floor. Without it, +6.4% reads as "the mechanism is backwards" — confident, publishable, wrong. **The arm went 4–2 on six games; so did the null arm.** -> log, `"the only reason I can say so"`
- **An engine-derived constant is still a DOSE.** `MOP_KEEP = 50` came from the engine's own cooldown band, which made it feel principled — but a mopper's tank is 100 and its measured lifetime upkeep is 62, so it sits below 50 most of its life and the lever fired ~1%. Check a threshold against the *distribution it will meet*, not only against the rule that motivated it. -> log, `"dosing the lever out of existence"`
- **A mechanism that did not fire cannot have caused the result beside it.** Lever B moved transfers by 1% and showed −69 paint actions; attributing that to the lever would have rejected a mechanism that never acted. Check firing before reading outcome — otherwise you close a direction you never tested. -> log, `"is not attributable to it"`
- **When mechanisms from unrelated measurements all price into the same band, the deficit is STRUCTURAL, not a bug.** Four levers — heading, upgrade priority, commitment, mopper transfer — derived from four independent measurements, all landed at 7–17% of the same gap. That is not four coincidences; it is arithmetic telling you no single lever fits. -> log, `"The band is the finding"`
- **A mechanism at your instrument's resolution returns null whether or not it works.** Iteration 53 moved its intermediate variable +639 (over 50% above the control) and returned net swept −2. So screening a ~16% lever on a 25-map/+4 screen spends 50 games to learn nothing. Check what your gate can resolve BEFORE screening: stack independent levers, or pay for a census. -> log, `"about my INSTRUMENT, not about the mechanisms"`
- **Never prefix `git add` to an `agent-commit.sh` call.** It commits through a private index and already handles new files, so the `git add` is redundant AND is the only part that can fail — and it fails on a wrong cwd *after* the heredoc that wrote the irreplaceable part. Three lost commits in one session. `tools/ac.sh` cds to the repo root itself. -> log, `"is redundant AND is the only part that can fail"`
- **A settled fact must carry its PROVENANCE.** "Ruins saturate" was established off one map, late, in self-play — then bore 44 iterations of weight and killed three of them. The provenance is what tells a future session how much a premise can carry; without it, a one-map observation reads exactly like a corpus census. -> log, `"Ruins do NOT saturate"`
- **A word that spans two measurable quantities will conflate them, every time.** "Saturation" named the ground filling with paint (true, 0-4% unpainted) and the ruins being built (false, 42-79% unbuilt) — and I took one as evidence for the other for 44 iterations. When a term covers two things you could count separately, count them separately. -> log, `"Ruins do NOT saturate"`
- **ONE SOLDIER CANNOT COMPLETE ONE TOWER — completion is a relay.** A finished tower takes 4.53 different alice soldiers and ~28 paint actions; a soldier delivers only ~20 to the ground in its whole life. Abandoned ruins got 2.41 soldiers and 16 paints — about 57% of the way. Alice's expansion is limited by soldier endurance at a ruin, not by ruin supply, discovery, or chips. -> log, `"PATTERN-COMPLETION limit"`
- **Compounding needs a purchase that raises income, not a ruin.** I argued chips stop compounding once expansion ends, so their sink is gone. But an *upgrade* is a sink that raises income (+10 chips/turn for 2,500), which funds everything else. I asked "can chips still buy a tower" and never "can chips buy a *better* tower". Iteration 26's rule re-confirmed under intervention. -> log, `"re-confirms iteration 26 under INTERVENTION"`
- **A non-latching signal cuts both ways, and I quoted only the good half.** "Not a latch" is a safety feature for reverting a block — and it also means the phase can *resume*. Iteration 53 throttled the income that funds expansion, then expansion resumed and could not be paid for. -> log, `"and I quoted that as a safety feature"`
- **Count the unit your file is written in, not its lines.** A results file writes three lines per game, so 58 lines looked like 50 games and was 19 — and the partial file showed the exact signature of an inert mechanism that was in fact engaging at 74%. Same error class as reading a dump at 41% that finished at 4.1%. And `pgrep -f "<run-id>"` matches your own polling loop, so it never returns 0. -> log, `"nearly read this run while it was still being written"`
- **Split every aggregate by the ACTOR before calling it a defect.** "23.4% of alice's paint actions repaint ground it already owns" looked like a headline waste; split by unit type it was **SPLASHER 6,488, SOLDIER 0** — the unavoidable footprint of an area weapon, not a decision any code makes. A rate with no actor attached cannot tell a bug from a geometry. -> log, `"killed by its own discriminator"`
- **A constraint on one unit type is not a constraint on the bot.** My pricing chain ended "post-saturation the only legal target is an empty tile a mopper made" — true for soldiers, false for the bot, because splashers overwrite live enemy paint directly (28–45% of post-r200 painting). Check whether *every* producer shares the constraint before making it the cap. -> log, `"corrects the last link of my own pricing chain"`
- **Use the OTHER opponent as a control — it was in my data the whole time.** Alice's "small-map problem" is a *carol-on-small-maps* problem: the size axis is −28.7 vs carol and dead flat vs bob. Four mechanisms and two probes were built on a framing one column of an existing table refuted. With three lineages, every deficit has a built-in control; use it before instrumenting anything. -> log, `"the size axis finally has an EXPLANATION"`
- **A per-turn tax is a lifetime x rate product — attack the duration, not the rate.** Alice's soldiers lose exactly half their tank to upkeep because they take 87.8 turns to spend it. Cutting the rate (iterations 49, 50) is worth 12% of the gap; cutting the duration to 40 turns is worth 50%. -> log, `"the lever is TIME"`
- **A bar registered against ZERO is not a bar when the statistic has a noise floor.** "Best of 8 sectors" is a maximum of eight noisy ~9-tile estimates, so it is upward-biased even with no structure at all: the winner's-curse floor was ~200 per mille and I had pre-registered 100. Simulate the null *before* registering the threshold, and void the gate rather than quietly replacing it. -> log, `"my registered bar was void"`
- **An internal consistency check catches instrument bugs that a plausible number hides.** `worst > chosen` is arithmetically impossible; it exposed a probe that recorded 0 for headings pointing off-map and inflated the headroom by ~140 per mille. Build one impossibility test into every counter. -> log, `"an internal consistency check caught a probe bug"`
- **Ask how much there is to COLLECT before asking whether it helps.** Eight games closed iteration 50 on a ceiling (17% of the gap under perfect assumptions); iterations 44–47 each spent a 50–150 game screen to learn the same class of thing. The bound comes from what is available, so no larger sample overturns it. -> log, `"The pre-check paid for itself"`
- **A gate blind to an effect carries no information about it — and section 8 is a pre-check whose trigger is BEFORE BUILDING.** I built a 6-game probe on self-play to explain a gap my own log had already measured as null inside my lineage (rho +0.014, p=0.903). Every pre-registered criterion passed; the probe reproduced the effect's ordering *backwards*, because the effect is opponent-driven and self-play cannot contain it. -> log, `"the direction DEAD, and the reason is my instrument"`
- **Say whether your claim needs the PER-TURN or the PER-LIFETIME number.** Soldier upkeep per turn is 1.7x higher on small maps; upkeep per lifetime is *lower*, because lifetime is 87.8 turns there against 220.0 on large. The two move oppositely and nearly cancel, so one framing looks like a finding and the other is flat. -> log, `"the hidden variable"`
- **A digest is a proxy for the engine, and mine was wrong.** `RULES.md` said the clumping tax ignored adjacent towers; the engine counts them (no `isRobotType()` filter, self excluded by ID only). Now also SIZED: 3.8–17.8% of soldier upkeep — real, and a minor term. -> log, `"adjacent ally TOWERS are taxed"`
- **A lesson written THREE times is not a lesson, it is a missing control.** Install the check where the mistake happens. -> `"a lesson written THREE times"`
- **A retraction is a claim too**, and it shares the assumption that produced the error. -> `"a retraction is a claim too"`
- **"Supersede in place" must ANNOTATE the old entry**, not merely append a new one. -> `"must ANNOTATE the old entry"`
- **Two rules in one document can disagree, and neither looks wrong alone.** -> `"two rules in one document can disagree"`
- **Consistency passes**: two separate sessions found that several of that day's entries were one error wearing different clothes. Run one before trusting a cluster of new lessons. -> `"CONSISTENCY PASS"`
- **Never redirect run output into the shared `/tmp` tree** — a sibling's run interleaved with mine. Use `agents/alice/logs/`. -> `"Instrument hygiene"`
- **Check a bar's DENOMINATOR and a proxy's DIRECTION before scoring anything against them.** B0's registered `union/(n*69)` demanded 5,727 tiles from a 3,600-tile board — unreachable by arithmetic, and the error pushed toward *spending*. And reporter-*turns* passed a bar of 3 at 13.4 while the registered quantity, distinct *reporters*, failed at 1.5–1.9 once divided by dwell. **An upper-bound proxy is honest for a KILL and inadmissible for a PASS.** -> log, `"The registered bar was mis-specified"`, `"Stage 2 FAILS at every plausible dwell"`
- **Passing a gate is not being the lever — compute what a direction wins at its BEST, and read the SHAPE of its marginal, not its mean.** B cleared B0 honestly and perfect spacing still moves empty-visibility only 6.4% -> ~10.4% against a 93.6% deficit. Conversely a marginal that runs 0.2 tiles on one map and **17.6 on the worst-famine map** is steepening where the problem is, which is the signature of a live lever rather than a saturating one. -> log, `"it passed its gate and is bounded anyway"`, `"the opposite of a saturating dead end"`
- **A unit-level optimum can hide a team-level famine — census the union, not the unit.** "The soldier is correctly idle" (an empty tile in action range on 1.8% of idle turns) reads as *the work is done*. At team level, 93.6% of paintable-empty tiles and 82% of bare ruins are invisible to the ENTIRE soldier team at once. Same bot, same data: the unit plays its information near-optimally and the information is 6% of the opportunity. **When a per-unit measurement says "no headroom", re-ask it over the union of all units before believing it.** -> log, `"what P1 actually meant"`
- **Check that a fix's PRECONDITION is met before choosing between fixes — and separate "failing to arrive" from "failing to aim".** On mit, 8–20 unclaimed sites sit for 700 rounds with the whole team seeing zero at every sample: **memory cannot remember what was never seen**, which kills a memory mechanism at its first stage and a spacing one on arithmetic. Separately, work sits a median 14 tiles from a soldier that walks ~1 tile/turn for ~88 turns — **99.4% could reach it, so travel was never the constraint; knowing which way is.** -> log, `"a finding that was in neither A nor B"`, `"Distance is not the barrier. Direction is."`
- **Put a control that CANNOT work, and a null arm, into every scoring harness — they are what make a verdict worth having.** An empty-tile signal scored 21.8% on soldiers with no empty tile in vision, exposing an east-biased tie-break; fixing it moved the candidate 1.16x -> 0.95x, and **a genuine fix moves a false positive toward the null**. And 50 control-vs-control games returning net **+0 exactly** are why a −8 is about the arm rather than the draw. **Ship the null in the same batch or the reject is an opinion.** -> log, `"a control that cannot work scoring above chance"`, `"NULL ARM = +0 EXACTLY"`
- **Write every closure's re-open condition as a TESTABLE SENTENCE — then a re-open costs a grep instead of an argument, and closing again for a different reason is progress, not churn.** Messaging closed on "no message worth sending", re-opened when a bearing acquired a measured referent, and re-closed on structure. **And notice where the conditions point: when every re-open condition for a direction is a change to a DIFFERENT subsystem, the direction is not a lever, it is a consequence of one.** -> log, `"discharges the messaging re-open condition"`, `"neither is a change to messaging"`
- **Selection effects hide in preconditions and in harness defaults — measure JOINTLY and check what your tooling kept.** BC25 messaging needs an ally-paint path, so every report originates inside already-explored territory and the soldiers who can reach a tower see **2.48x fewer** empty tiles than those who cannot: the marginals looked fine, the joint measurement did not. And the gauntlet keeps the *bot's* losses — my bot was the control, so every retained replay was a game the **arm won**, making all five upstream links read positive against a −8 outcome. **An outcome-conditioned sample makes every upstream link look good, and the flattering reading is always the available one.** -> log, `"the channel selects its reporters"`, `"My own sampling note had it backwards"`
- **"The enumeration is complete" is a CLAIM, and so is "this cap binds" — check both.** My four closed routes all varied *how well existing units use information*; none varied *how much information exists*, which was the only live leg. And the cap I would have named as limiting the army — spawn cooldown — sits at **15–22% utilisation**, five times from binding, while paint income actually binds. **Write down the axis every route varied and ask what axis it isn't; a cap you have not measured is a guess wearing an engine constant's clothes.** -> log, `"my enumeration was incomplete"`, `"NOT binding, and not close to binding"`
- **When every re-open condition for a direction is a change to a DIFFERENT subsystem, the direction is not a lever — it is a consequence of one.** Both times messaging closed, re-opening it required something else to change first (a payload worth sending; a paint trail keeping explorers connected). Spend the effort on the subsystem the conditions keep pointing at. -> `"neither is a change to messaging"`
- **The escape from a two-currency category error is a SURPLUS measurement, not a better argument — but a surplus has a TIME PROFILE.** Iteration 34 cost a census by comparing compounding chips to consumable paint with a per-turn rate; the way out was that alice holds a mean **42,712 chips** while its towers cannot afford one 200-paint soldier, and no comparison is needed. **But chips average 1,502 in rounds 100–300 and 56,706 late, while the r300 lead predicts 79–81% of winners — so the pooled surplus is a shortage in the phase that decides. Split a claimed surplus by the phase your win condition lives in.** -> log, `"the iteration-34 trap is disarmed by measurement"`, `"surplus I priced the mechanism against is a LATE-game surplus"`
- **A discriminator separating "can't" from "won't" is worth more than the rate it qualifies.** 82.9% of towers being paint-starved is suggestive; **at-cap 0.0% across 2,542 frames** is decisive, because a tower that was *choosing* not to spawn would pile paint against its 1000 cap. Whenever a resource looks scarce, check whether it is also being *accumulated* — that single number tells you which hypothesis you are in. -> log, `"Towers are not declining to spawn; they are broke"`
- **A plateau that closes into a LOOP is not a ceiling — find the entry point.** Can't see ruins -> few towers -> half of them make no paint -> towers starved -> spawning throttled to 15-22% of the engine's ceiling -> few soldiers -> small vision union -> can't see ruins. Every arc was measured separately and none of them is a wall. **When your closures start reinforcing each other, draw the cycle: a loop has an entry point and a ceiling does not.** -> log, `"It is a feedback loop"`
- **A pre-registration fixes the thresholds but NOT the order you test them in — put the branch order in code, then arrange never to see a partial number.** Deciding which of ACCEPT/REJECT/VOID to test first with the numbers on screen is how a void becomes whichever verdict the author preferred. `tools/e2-gate.py` fixes the order; blocking on the completion marker removes the chance to peek. **A control that does not depend on your continuing discipline beats the discipline — the discipline is what fails on the ninetieth game — and two protections resting on the same person are one protection.** -> log, `"the VOID branch is tested first, in code"`
- **Commit your interpretation before the number: state the uncomfortable detail first, and confirm the MECHANISM separately from the OUTCOME.** mit unchanged was a reachability limit sitting on the worst-deficit map — said before the number that is a caveat, after a reject an excuse, after an accept a footnote nobody reads. And E2's realised share moved +10.3 points with +17% units *before* the screen returned, which is what makes its reject informative: **the design failed, not an inert branch** — and inert branches are what most of this lineage's rejects have actually been. -> log, `"cannot fire where the bot never gets to make the choice"`, `"This is the *mechanism* confirmed, not the *outcome*"`
- **The PROSPERITY-GATED LEVER: a lever keyed to a choice the bot only faces when it is already doing well is structurally absent exactly where the deficit lives — and "where" can be a PHASE as well as a map.** Tower *mix* is a choice about towers that already exist; on the worst-famine map alice never holds more than four, and in the decisive window it holds 4.2, so 16 points of type-share is 0.7 towers. **The lever's reach is anti-correlated with the need for it. Compute its denominator inside the decision window and on the worst-deficit maps specifically** — a corpus mean and a whole-game mean hide the same thing in different clothes. -> log, `"The lever's reach is anti-correlated with the need for it"`, `"where the deficit lives" is a **phase**`
- **A lever has a TIME CONSTANT — compare it to the window that decides the outcome, not to the length of the game.** E2's mix change compounds at 5–15 paint/turn against a 200-paint unit, so it is genuinely ahead by r600 and indistinguishable from the control through r300 — and r300 predicts 79–81% of winners. **A mechanism that works, too late, is a losing mechanism**, and the arithmetic to catch it is available before any build.
- **A rate that stays FLAT while its supply varies identifies the rate limiter — no experiment needed.** Bare ruins visible to alice sit at 1.62 / 1.56 / 1.56 across r100–300 while the stock on the map falls 15.75 → 7.50. A supply-limited process tracks its supply; this one does not, so the limit is *discovery*, not availability. **Vary the input you did not choose and see whether the output notices** — it is the cheapest causal test available and it runs on data you already have. -> log, `"Discovery is the rate limiter"`
- **Score a lever on the regime that has room, and identify the regimes first.** Of 8 maps, 3 are EXHAUSTED (zero ruins left — the ceiling is already reached and no mechanism can help), 3 STARVED, 1 rich. Pooling them averages a lever against maps where improvement is impossible and reports it as weak. This is the corpus-mean error in its third costume; the first two were map-averaging and whole-game-averaging. -> log, `"Any future lever must be scored on the starved regime specifically"`
- **Count how many times your lever gets to ACT inside the window that decides, and prefer the largest denominator.** E2 attached itself to the tower-type choice: **~5 opportunities in r1–300**, against ~30 spawns and **~2,700 soldier movement-turns**. I picked the rarest decision in the game during the phase when it is rarest, and no tuning of the constant could have survived that. **Enumerate the decision sites with their counts before choosing one.** -> log, `"I attached a lever to the rarest decision in the game"`
- **A refutation has a SCOPE — re-read what your own test's "truth" actually was before treating a kill as general.** C2 killed the own-paint gradient at 0.95x random, but its truth was the first step of the *shortest walk to the nearest empty tile*: a targeting criterion. That refutes **aiming**, not **spreading**, and I had been citing it as if it closed both. **Dead as a targeting signal, untested as an exploration signal** — and the conflation was mine. -> log, `"it refutes *aiming*, not *spreading*"`
- **The recurring error of this lineage, named:** a real number attributed to a referent wider than it supports. It has appeared at least five times (sparseness-for-size, skipM, the mopSwing constant, the gauntlet-blindness overclaim, b0=0). When a number resolves an open problem on first contact, that is when to check the referent, not when to celebrate.
- **This lineage's failure mode is generating mechanisms faster than it locates defects.** Iterations 44–47 were four mechanisms proposed without a located defect, and all four were rejected. Census before mechanism.
